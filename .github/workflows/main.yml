name: CI
on: [push]
jobs:
    build_and_test_frontend:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout repository and change directory to frontend
              uses: actions/checkout@v2

            - name: 🗂️ Install
              run: |
                  cd frontend
                  npm install

            - name: 🧪 Test  npm test
              run: |
                  cd frontend
                  npm test
