name: CI
on: [push]
jobs:
    build_and_test:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v2

            - name: Change directory to frontend and run tests
              working-directory: ./frontend
              run: |
                  npm install
                  npm test
                  CI="" npm run build
